*   Using https://github.com/dcramer/django-uuidfield to provide uuidfields for models. Seems fairly highly recommended and better than using a CharField with default set via a UUID generating function.

*   Code is attempted to keep within 80-wide lines, to allow a 3-coloum layout for working on code.